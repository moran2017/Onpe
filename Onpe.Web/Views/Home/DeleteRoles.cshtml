@model Onpe.Web.ViewModel.DeleteRolesViewModel

@{
    ViewBag.Title = "Eliminar Rol";
}
    

<div class="alert">
    <p>
        <h3>Estas seguro que deseas eliminar el Rol?</h3>
    </p>
</div>
<hr />
<div class="row" align="center">
    <div class="col-md-4">
        <table class="table table-hover table-striped">
            <thead>
                <tr>
                    <th> @Html.DisplayNameFor(model => model.RolId)</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>@Html.DisplayFor(model => model.RolId)</td>
                </tr>
            </tbody>
        </table>
    </div>
</div>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-actions">
        <button type="submit" value="Delete" class="btn btn-primary">
            <i class="fa fa-trash"></i>&nbsp;Eliminar
        </button>
        @Html.ActionLink("Back to List", "LstRoles")
    </div>
}
</div>
